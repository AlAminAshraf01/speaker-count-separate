# Runtime requirements.
#
# On Kaggle, EVERY ONE of these is already installed in the default Python image --
# you do not need to pip install anything, and you should not, because a pip install
# inside a GPU notebook costs minutes of your quota and can break the CUDA build of torch.
#
# This file exists so the repo runs locally too (CPU is fine for everything except
# 04_train.py / 05_hparam_search.py at full size).
#
#   python -m pip install -r requirements.txt
#
# For a CPU-only local install of torch, prefer:
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cpu

numpy>=1.24
scipy>=1.10
torch>=2.0
soundfile>=0.12
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65
PyYAML>=6.0
pyloudnorm>=0.1.1

# Deliberately NOT used anywhere in this repo -- see docs/CONTRACT.md section 14:
#   asteroid, librosa, torchaudio, speechbrain
# They are heavy, they pin torch versions, and everything we need from them is
# implemented directly in src/csnet (Conv-TasNet, PIT, SI-SDR, STFT baselines).
